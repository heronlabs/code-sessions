name: Release

on:
  push:
    branches:
      - main

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: googleapis/release-please-action@a02a34c4d625f4a02a4b24fa08ea2e5a7da23629
        with:
          release-type: simple
